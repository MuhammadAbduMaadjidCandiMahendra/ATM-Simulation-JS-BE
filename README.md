# Run Backend

## Prerequisites

- Java JDK 17 must be installed.

## Run the Application

### Linux / macOS

Grant execute permission:

```bash
chmod +x mvnw
```

Run the application:

```bash
./mvnw spring-boot:run
```

### Windows

```bat
mvnw.cmd spring-boot:run
```
